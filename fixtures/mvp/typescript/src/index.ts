import { value } from "./value";

// Noise/comment lines are intentionally ignored by the syntax observer.
export interface Demo {
  id: string;
}

export const load = () => value;
export const answer = 42;
